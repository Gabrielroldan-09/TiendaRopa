{
  "name": "tienda-ropa",
  "version": "1.0.0",
  "main": "backend/src/app.js",
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd backend && npx prisma generate && npx prisma db push || true",
    "start": "cd backend && npx prisma db push && node src/app.js"
  }
}